using UnityEngine;

public class CreatureSpriteManager : SpriteManager
{
    [SerializeField]
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        // Initialize any necessary components or settings
        Debug.Log("Creature Sprite Manager Initialized");
    }

    // Update is called once per frame
    void Update()
    {
        // Handle any updates related to creature sprites
    }
}

